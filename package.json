{
  "name": "draino",
  "version": "0.0.2",
  "description": "Drains randomly generated crypto wallets (Sepolia Testnet)",
  "main": "src/index.js",
  "license": "MIT",
  "scripts": {
    "start": "node src/index.js",
    "setup": "npm install && mkdir -p logs",
    "clear": "rm -f ./logs/*"
  },
  "dependencies": {
    "ethers": "^5.4.7"
  }
}
