{
  "name": "draino",
  "version": "0.1.0",
  "description": "Drains randomly generated crypto wallets (Sepolia Testnet)",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node index.js",
    "setup": "npm install && mkdir -p logs",
    "clear": "rm -f ./logs/*"
  },
  "dependencies": {
    "ethers": "^5.4.7"
  }
}
